node_modules/
dist/
.astro/
.env
.env.*
!.env.example
*.log
.DS_Store
Thumbs.db
.vercel
.netlify
publish-manifest.json

# Generated by scripts/dashboard.mjs — a local snapshot, never committed.
.blog-dashboard.html

# Local working notes, not part of the site. Force-add if one should be tracked.
/CONTEXT-INVENTORY.txt
/EDITS-TO-APPLY.md

# routines/ headless-runner output — per-run transcripts and the telemetry log.
/routines/runs/*
!/routines/runs/.gitkeep
/routines/runs.jsonl

# manager watchdog state + log
/routines/.manager-state.json
/routines/manager.log
